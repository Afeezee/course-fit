# Minimal exclusions only. Anything more aggressive has tripped
# Railway's BuildKit checksum step in earlier attempts.

.git
node_modules
apps/web/node_modules

# Never bake local secrets into the image
.env
.env.local
apps/api/.env
apps/api/.env.local
apps/web/.env
apps/web/.env.local
